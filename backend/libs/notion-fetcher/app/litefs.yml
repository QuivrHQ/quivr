fuse:
  dir: "/litefs"

data:
  dir: "/var/lib/litefs"

lease:
  type: "static"
  advertise-url: "http://notion-fetcher:20202"
  candidate: $IS_PRIMARY

exec:
  - cmd: "uvicorn main:app --host 0.0.0.0 --port 8000"
